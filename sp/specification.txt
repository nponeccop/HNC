function_name :== <a-z>+<0-9>*

, - call function,
	example:
		,sum 1 2 = 3
		,sum 1 = ,sum 1
		,sum 1 2 3 = `error

I think multiple params in not neccesary

condition := if[...]
	example:
		if[,ln f     ; 1
		   ,more 1 2 ; 2
			     ; 3]

